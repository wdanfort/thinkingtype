"""Decision-gate experiment pipeline (v1).

Tests whether rendering identical content as an image moves real gate
decisions (resume screen, moderation, benefits appeal) on cases calibrated
to each model's own decision boundary.
"""
